# Kernel References

Open kernel-side camera references were used during local research and can be curated into this directory as the public repo is finalized.

This placeholder keeps the target repo structure intact without copying proprietary camera payloads.